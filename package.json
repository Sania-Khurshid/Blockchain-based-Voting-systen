{
  "name": "decentralized-voting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run compile-contracts && browserify src/js/app.js -o src/dist/app.bundle.js && browserify src/js/login.js -o src/dist/login.bundle.js",
    "compile-contracts": "truffle compile || echo 'Warning: Contract compilation failed. Make sure Ganache is running and internet connection is available.'",
    "start": "node index.js",
    "dev": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "@truffle/contract": "^4.6.18",
    "browserify": "^17.0.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "web3": "^1.9.0"
  }
}
